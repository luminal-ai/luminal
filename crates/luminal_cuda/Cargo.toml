[package]
name = "luminal_cuda"
version = "0.2.0"
edition = "2021"
description = "Cuda compiler for luminal"
license = "MIT OR Apache-2.0"

[dependencies]
luminal = { path = "../.." }
cudarc = { version = "0.16.6", features = [
    "f16",        # Required for INT8 scale factors
    "cuda-11080", # CUDA 11.8 for AWS Deep Learning AMI
    "cudnn"       # Required for INT8 optimizations
] }
itertools = "0.12.1"
rustc-hash = "2.1.1"
half = "2.3.1"        # Required for INT8 quantization

[dev-dependencies]
rand = "0.9.2"
paste = "1.0.14"
luminal_nn = { path = "../../crates/luminal_nn" }